import { Container } from '~/components';
import Form from '~/sections/home';

const Home = () => {
  return (
    <Container maxWidth="sm">
      <Form />
    </Container>
  );
};

export default Home;
